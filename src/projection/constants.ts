/**
 * Tile size in logical pixels
 */
export const tileSizePx = 256;

/**
 * World size in map points
 *
 * ⚠ WARNING: Stable compatibility with the MapGL API is not guaranteed. The constant depends on the internal map logic that may be changed in the future.
 */
export const worldSize = 2 ** 32;
